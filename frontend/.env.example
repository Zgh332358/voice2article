# 后端 API 基地址。开发模式下走 vite 代理 /api，可保持空。
# 生产打包时填入完整后端地址，如 https://api.example.com/api/v1
VITE_API_BASE_URL=

# vite dev server 把 /api/* 代理到这个地址。默认 8000，
# demo-up.sh 会写一份 .env.local 覆盖到正确端口。
VITE_BACKEND_URL=http://127.0.0.1:8000
